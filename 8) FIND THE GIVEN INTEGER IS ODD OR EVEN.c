#include <stdio.h>

void main() {
    int num;

    printf("Enter an integer: ");
    scanf("%d", &num);

   if {
        printf("%d is even", num);
    } else {
        printf("%d is odd", num);
    }

    return 0;
}
